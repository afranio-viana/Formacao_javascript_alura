const nomes = ['Afrânio', 'esquerdO', 'vIana'];

/*Aqui o map foi utilizado para padronizar
as strings de dentro de um array*/
const nomesPadronizados=nomes.map((nome)=>nome.toUpperCase());

console.log(nomesPadronizados);